# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8

# 多线程构建：多模块（hidden_apis / priv_library / priv_daemon / app）并行编译。
org.gradle.parallel=true
# 编译工作进程数上限。
org.gradle.workers.max=8
# 构建缓存，增量构建复用上一次的任务输出。
org.gradle.caching=true

# 注意：这里不要写 systemProp.*proxyHost / proxyPort。
# 那是本机专用的设置，写进版本库会让 CI runner 去连 127.0.0.1:10808 而拉不到依赖。
# 本地需要代理时放到用户级配置（不入库）：
#   ~/.gradle/gradle.properties  (Windows: %USERPROFILE%\.gradle\gradle.properties)
# 例如：
#   systemProp.http.proxyHost=127.0.0.1
#   systemProp.https.proxyHost=127.0.0.1
#   systemProp.http.proxyPort=10808
#   systemProp.https.proxyPort=10808
#   systemProp.http.nonProxyHosts=localhost|127.0.0.1
#
# 依赖不再走国内镜像（settings.gradle.kts / buildSrc/settings.gradle.kts 只用官方原始仓库），
# 国内网络的加速由 ./gradlew downloadDeps 预取到 libs/maven-repo 来提供；
# downloadDeps 是普通 JVM 下载，同样吃上面的代理设置。

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
#android.enableJetifier=true

# Improve Compose preview rendering performance.
#org.gradle.configuration-cache=true

# Show deprecated Gradle features and obsolete APIs
org.gradle.warning.mode=all
android.debug.obsoleteApi=true

#android.enableR8.fullMode=true
